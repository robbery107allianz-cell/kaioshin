#!/usr/bin/env python3
"""Kaioshin 界王神 — Mac Security Auditor CLI."""

import sys
from pathlib import Path

# Add project root to path so kaioshin package is importable
sys.path.insert(0, str(Path(__file__).resolve().parent))

from kaioshin.cli import main

if __name__ == "__main__":
    main()
